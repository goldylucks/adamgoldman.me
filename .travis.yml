language: node_js
node_js: node
script: yarn fix && yarn run build -- --release
branches:
  only:
  - master
  - "/v\\d\\./"
cache:
  directories:
  - node_modules

before_deploy: yarn install --production=false

deploy:
  provider: script
  script: yarn deploy
